integrator|You are the integrator. IMPORTANT: every command that may block must run under a hard alarm, exactly like this: `perl -e 'alarm 180; exec @ARGV' python3 -m unittest discover -s tests -v` and `perl -e 'alarm 180; exec @ARGV' bash smoke.sh`; never run the server or tests without the alarm wrapper, and if a command is killed by the alarm treat it as a hang bug in server.serve or in the test (a subprocess waiting on stdout that never comes) and fix the cause. First run the wrapped unittest. Read SPEC.md and every file under quarry_mcp/ and tests/. `quarry_mcp/cli.py`, `__main__.py` and `smoke.sh` may be incomplete or missing: finish them per SPEC.md (cli: `--db PATH`, subcommands `serve` and `call <tool> <json-args>`; smoke.sh: a bash script that runs an inline Python driver which spawns `python3 -m quarry_mcp --db fixtures/quarry-fixture.sqlite serve`, sends initialize, notifications/initialized, tools/list, then tools/call for quarry.stats, quarry.search {"query":"Poisson"}, quarry.get on KNOWN_ID from tests/conftest_fixture.py with ["informal","edges","metric"], quarry.closure on it (deps), quarry.similar on it, and one invalid JSON line, each with a 30 s read timeout, recording latency ms and pass/fail per step into logs/smoke.json and printing a table). Fix every failing test by correcting the implementation, or the test only where it contradicts SPEC.md. Iterate until the wrapped unittest and the wrapped smoke.sh are both green, then write REVIEW.md: what was broken, what you changed, remaining risks. Be economical: read each file once, one consolidated edit per file.
