architect|You are the software architect of a 5-agent team. Read BRIEF.md in the current directory completely: it defines the product (quarry_mcp, an MCP JSON-RPC server over stdio on a SQLite registry of Lean declarations) and the fixture database. Inspect the fixture schema with a short Python snippet via the shell tool (sqlite3 module; print `SELECT sql FROM sqlite_master`) and run two sample queries so your spec matches reality (an FTS MATCH on decl_fts for "Poisson", and the edge rows for id "github.com/anthropics/fermats-last-theorem@aa2d8b34/SchwartzMap.tsum_eq_tsum_fourier_euclideanSpace"). Then write, with write_file: (1) SPEC.md: package layout `quarry_mcp/{__init__,db,tools,protocol,server,cli,__main__}.py` and `tests/{__init__,test_db,test_protocol,test_e2e}.py`; exact public signatures: `db.Registry(path)` with methods `search(query, mode="text", corpus=None, kind=None, limit=10, exclude_tags=None) -> list[dict]`, `get(id, with_=()) -> dict`, `closure(id, direction="deps", depth=50, stop_at_corpus=None, kind="uses") -> dict`, `similar(id=None, statement=None, limit=10) -> list[dict]`, `stats() -> dict`, each raising `db.NotFound(id)` for unknown ids and `db.BadArgument(msg)` for invalid arguments; `tools.TOOL_SPECS` (list of MCP tool descriptors with JSON Schema inputSchema for the five tools named in BRIEF.md) and `tools.dispatch(registry, name, arguments) -> dict`; `protocol.handle(request: dict, registry) -> dict | None` implementing initialize, notifications/initialized (returns None), tools/list, tools/call, with JSON-RPC error objects (-32700 parse, -32600 invalid request, -32601 method not found, -32602 invalid params); `server.serve(registry, stdin, stdout)` reading newline-delimited JSON, writing one JSON line per response, logging to stderr, surviving invalid JSON lines by emitting a -32700 error; `cli.main(argv)` with `--db PATH` and subcommands `serve` and `call <tool> <json-args>`. Specify exact result shapes for every tool as in BRIEF.md, including field names, truncation and ordering rules, and the FTS5 query-quoting rule for `similar`. (2) README.md with usage. (3) pyproject.toml (name quarry-mcp, version 0.1.0, no dependencies, script quarry-mcp = "quarry_mcp.cli:main"). (4) The full skeleton: every module with docstring and NotImplementedError stubs matching the signatures, tests/__init__.py, and `tests/conftest_fixture.py` exposing FIXTURE = path of fixtures/quarry-fixture.sqlite relative to the repo root plus KNOWN_ID = the SchwartzMap id above. Be precise; four other agents implement and test from SPEC.md without talking to you. Finish by listing the directory.
