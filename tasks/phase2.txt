impl-db|You are the database implementer. Read SPEC.md and BRIEF.md. Implement ONLY `quarry_mcp/db.py` fully (Registry with search/get/closure/similar/stats, NotFound, BadArgument), standard library sqlite3 only, parameterized SQL, read-only connection (`file:...?mode=ro` URI). search text mode: FTS5 MATCH on decl_fts joined to decl and LEFT JOIN metric, ORDER BY in_degree DESC, rank; statement truncated to 300 chars; title from informal. closure: iterative BFS over edge with the given kind, depth limit, stop_at_corpus rule from SPEC, results capped at 2000 ids, by_corpus and by_module counts computed from decl rows (unknown ids counted under corpus "unknown"). similar: exact stmt_hash matches in other corpora first, then FTS on identifier tokens of the statement (tokens containing a dot or length >= 4, each double-quoted, joined with OR, at most 12 tokens). Do NOT touch other files. Before finishing, run with the shell tool: python3 -c "from quarry_mcp.db import Registry; r=Registry('fixtures/quarry-fixture.sqlite'); print(r.stats()); print(r.search('Poisson')[:2]); print(r.closure('github.com/anthropics/fermats-last-theorem@aa2d8b34/SchwartzMap.tsum_eq_tsum_fourier_euclideanSpace')['count'])" and fix any error.
impl-protocol|You are the protocol implementer. Read SPEC.md and BRIEF.md. Implement ONLY `quarry_mcp/tools.py` (TOOL_SPECS with proper JSON Schema inputSchema for the five tools, and dispatch that validates required arguments and types, maps db.NotFound/BadArgument to a tool error result with isError true), `quarry_mcp/protocol.py` (handle: initialize, notifications/initialized -> None, tools/list, tools/call, JSON-RPC 2.0 error objects with the codes in SPEC; result of tools/call is {"content":[{"type":"text","text": json.dumps(result)}],"isError": false}) and `quarry_mcp/server.py` (serve loop: for each line on stdin, parse JSON, on failure write a -32700 error response with id null and continue; write responses as single lines with flush; never print anything else to stdout; log exceptions to stderr; return on EOF). Import db names as declared in SPEC.md even if db.py is still a stub. Do NOT touch db.py, cli.py or tests/. Before finishing run: python3 -c "import quarry_mcp.protocol, quarry_mcp.server, quarry_mcp.tools; print('ok')" and fix errors.
impl-cli|You are the CLI implementer. Read SPEC.md and BRIEF.md. Implement ONLY `quarry_mcp/cli.py` and `quarry_mcp/__main__.py`: argparse with global `--db PATH` (required), subcommand `serve` (constructs db.Registry and calls server.serve(registry, sys.stdin, sys.stdout)) and `call <tool> <json-args>` (parses the JSON args, calls tools.dispatch, prints json.dumps(result, indent=2), exit code 1 on tool error, 2 on usage error). Also write `smoke.sh`: a bash script that starts `python3 -m quarry_mcp --db fixtures/quarry-fixture.sqlite serve` as a subprocess (use a small inline Python driver, not bash pipes, so you can read responses), sends initialize, notifications/initialized, tools/list, then tools/call for quarry.stats, quarry.search {"query":"Poisson"}, quarry.get with the KNOWN_ID from tests/conftest_fixture.py and with ["informal","edges","metric"], quarry.closure on it (deps), quarry.similar on it, and an invalid JSON line; it must record per-call latency ms and a pass/fail per step into logs/smoke.json and print a table. Do NOT touch db.py, protocol.py, server.py, tools.py or tests/. Before finishing run `python3 -m quarry_mcp --help` and fix errors.
tester|You are the test engineer. Read SPEC.md and BRIEF.md and tests/conftest_fixture.py. Write ONLY `tests/test_db.py`, `tests/test_protocol.py` and `tests/test_e2e.py` with unittest against the real fixture (no mocks for the DB). test_db: stats has three corpora; search "Poisson" (text) includes KNOWN_ID within the first 10 results; search with corpus filter only returns that corpus; get(KNOWN_ID, with_=("edges","informal","metric")) has non-empty edges.uses; get unknown id raises NotFound; closure(KNOWN_ID, "deps") has count > 0 and by_corpus keys; closure with stop_at_corpus set to the Mathlib corpus id does not expand Mathlib nodes (count strictly smaller or equal, and no Mathlib ids appear as sources of further expansion: assert count <= unbounded count); similar(id=KNOWN_ID) returns a list whose items have a "match" key; BadArgument on an invalid mode. test_protocol: handle(initialize) has serverInfo.name == "quarry-mcp"; handle(notifications/initialized) is None; tools/list returns exactly five tools with the names from BRIEF.md; tools/call quarry.stats returns content[0].type == "text" whose text parses as JSON; unknown method -> error.code -32601; tools/call with missing required argument -> isError true or -32602 (accept either, per SPEC); test_e2e: spawn `python3 -m quarry_mcp --db fixtures/quarry-fixture.sqlite serve` with subprocess, send initialize + tools/list + one tools/call + one invalid JSON line + one more valid call, assert the invalid line yields error.code -32700 and the server keeps answering; kill the process in tearDown. Do NOT modify quarry_mcp/. Finish by running `python3 -m unittest discover -s tests -v` once and reporting the result (failures are expected if implementers are not done).
